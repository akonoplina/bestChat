const initialState = {
  userName: 'Anastacia',
  userAvatar: 'tyty',
  userAge: '30',
  userAboutMe: 'Just another junior UI dev)))'
};
export default function userReducer(state = initialState, action) {
    return state;
}